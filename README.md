# cs81-module3a-github
emma-experiment

This program defines the following functions:

greet(): A function to fetch a user's name and greet them by name \n
fetchAge(): A function to fetch and print a user's age \n
calcAge(): A function to calculate and print a user's age in 20 years \n
fetchColor(): A function to fetch and print a user's favorite color \n
goodbye(): A function to print a personalized goodbye message \n
